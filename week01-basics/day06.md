# 第 1 周 Day 06 - 2026.09.23

## 今天学了
- 补上了数组（之前顺延两次）：定义、静态/动态初始化、遍历（for / 增强 for）
- 数组常见操作：求最大值、最小值、平均值、统计偶数个数
- 跟堂敲了课程代码，并独立完成了数组练习
- 数组越界（ArrayIndexOutOfBoundsException）：合法下标是 0 ~ length - 1

## 力扣第一题「两数之和」
- 大体思路正确（两层循环枚举配对），自己动手写了暴力版
- 写的时候出了不少错：
  - `=` 和 `==` 混淆（判断相等要用 `==`）
  - 内层循环条件写成了 `i`，导致死循环
  - 循环边界 `length - 2` 写错，会漏元素
  - 打印了字符串字面量 `"[i,j]"` 而不是变量值
  - 方法声明返回 `int[]` 但没有 `return`
- **`return new int[]{i, j}` 还不理解** → 明天结合「方法」一起搞懂
- 反思：应该还是练习得太少，看懂和自己写出来差距很大

## 没完成的
- 方法（定义、参数、返回值、重载）→ **顺延到明天**
- LeetCode 还没提交（先改 bug 跑通）

## 明天计划（9.24 周四，week01 最后一天）
- [ ] 方法：定义、参数、返回值、重载
- [ ] 搞懂 `return new int[]{i, j}` 是什么意思（动态初始化 + 返回数组）
- [ ] 跑通力扣两数之和 + 移动零
- [ ] 方法练习（isEven / max / 重载 / 把数组最大值平均值改写成方法）
- [ ] 周复盘
- [ ] 掘金第 1 篇博客
